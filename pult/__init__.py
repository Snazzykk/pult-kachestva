"""Пульт качества — локальный дашборд состояния тест-фундамента проекта.

Пакет со всей серверной частью инструмента:

  core     — чтение/запись quality-state.yml (+ .bak при каждой записи)
  yamlio   — минимальный YAML без внешних зависимостей
  textenc  — определение кодировки файлов (utf-8 / cp1251 / …) для импортёров
  openapi  — разбор OpenAPI/Swagger → список операций (импорт в фичи)
  tmsio    — разбор CSV/XLSX-выгрузки TMS → таблица + маппинг колонок
  server   — http.server (stdlib) — отдаёт web/index.html и JSON API

Только стандартная библиотека; PyYAML в core.py — опционален (используется,
если установлен, для более устойчивого чтения ручных правок).

Точки входа — run.py и build.py в корне репозитория, не в пакете.
"""
from __future__ import annotations

__version__ = "0.5.0"
