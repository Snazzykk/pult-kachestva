# «Пульт качества» работает без внешних зависимостей (только стандартная библиотека Python 3.9+).
#
# PyYAML — опционально: если установлен, используется для ЧТЕНИЯ quality-state.yml
# (устойчивее к ручным правкам). Запись всегда идёт через встроенный yamlio.
#
#   python -m pip install pyyaml
PyYAML>=6.0 ; extra == "yaml"
